default_platform(:android)

  platform :android do

#   desc "Build"
#   lane :build do
# #   |options|
# #     build_variant = options[:build_variant] || 'release' # Default to 'release' if no variant is specified
#     gradle(
#       task: "clean assemble",
#       build_type: "debug"
#     )
#
#   end
#
#   desc "Runs all the tests"
#   lane :test do
# #   |options|
# #     build_variant = options[:build_variant] || 'debug' # Default to 'debug' if no variant is specified
#     gradle(
#       task: "test",
#       build_type: "debug"
#     )
#   end
  lane :set_version_name do
    version_name = "5.0.0" # Change this to the desired version name
    gradle(task: "setVersionName", properties: { versionName: version_name }, build_type: "debug")
  end

  lane :set_version_code do
    version_code = 5 # Change this to the desired version code
    gradle(task: "setVersionCode", properties: { versionCode: version_code }, build_type: "debug")
  end


  lane :deploy_to_firebase do

    gradle(task:"clean assemble", build_type: "debug")

    firebase_app_distribution(
        app: "1:900612138286:android:a321b7388df40ea76bc6bf",
        release_notes: "HAHAHA",
        firebase_cli_token: "1//0g2YwJ-ZSs1BgCgYIARAAGBASNwF-L9Irk-jUQlzHZuP9x1-3ckcjDIwzjxi5hUxrs_Maydo2_jduZB3vQHHQjYJPmcS_kR5bM6w"
    )
  end

end